services:
  zwave-js-ui:
    image: ghcr.io/scottdkey/zwave-js-ui:latest
    container_name: zwave-js-ui
    restart: unless-stopped
    ports:
      - "8091:8091"
    volumes:
      - /opt/zwave-js-ui/config:/config
